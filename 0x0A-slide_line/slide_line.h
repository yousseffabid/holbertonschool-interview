#ifndef SLIDE_LINE
#define SLIDE_LINE
#define SLIDE_LEFT 2
#define SLIDE_RIGHT 1
#include <stdio.h>

int slide_line(int *line, size_t size, int direction);
int slide_man(int *line, size_t size, int direction);

#endif